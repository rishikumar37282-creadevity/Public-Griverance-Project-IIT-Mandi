# Public Grievance Urgency Classification System
# Install PyTorch CPU wheel first (or use the default index for CUDA builds):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
openpyxl>=3.1
fastapi>=0.110
uvicorn[standard]>=0.29
# optional — only for re-running the notebook
matplotlib>=3.8
seaborn>=0.13
