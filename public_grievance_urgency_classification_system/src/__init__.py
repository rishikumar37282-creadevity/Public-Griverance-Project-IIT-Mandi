"""Public Grievance Urgency Classification System — ML package.

Ports the BiLSTM + Attention training notebook into reusable modules:
config, urgency oracle (weak supervision), preprocessing, model,
training with CSV experiment logging, and inference with attention.
"""
